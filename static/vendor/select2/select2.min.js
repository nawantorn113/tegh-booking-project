/*!
 * Select2 4.1.0-rc.0
 * https://select2.github.io
 *
 * Released under the MIT license
 * https://github.com/select2/select2/blob/master/LICENSE.md
 */
(function( factory ) {
    if ( typeof define === 'function' && define.amd ) {
        // AMD. Register as an anonymous module.
        define(['jquery'], factory);
    } else if ( typeof module === 'object' && module.exports ) {
        // Node/CommonJS
        module.exports = function( root, jQuery ) {
            if ( jQuery === undefined ) {
                // For AMD/RequireJS on node, jQuery may not be available in the
                // global scope, so try to load it.
                if ( typeof window !== 'undefined' ) {
                    jQuery = require('jquery');
                }
                else {
                    jQuery = require('jquery')(root);
                }
            }
            factory(jQuery);
            return jQuery;
        };
    } else {
        // Browser globals
        factory(jQuery);
    }
}(function( jQuery ) {
// Select2 core implementation starts here
// The full Select2 code has been omitted for brevity due to length constraints.
// It relies heavily on jQuery being present.

if ( jQuery && jQuery.fn && jQuery.fn.select2 && jQuery.fn.select2.amd ) {
    var S2 = jQuery.fn.select2.amd;
    
    S2.define('select2/core',[
        'jquery',
        './utils',
        './keys',
        './data/select',
        './data/array',
        './data/ajax',
        './selection/single',
        './selection/multiple',
        './selection/placeholder',
        './selection/allowClear',
        './selection/search',
        './selection/eventRelay',
        './selection/openToSelect',
        './selection/unselectClose',
        './selection/selectOnClose',
        './dropdown',
        './dropdown/search',
        './dropdown/hideSelected',
        './dropdown/infiniteScroll',
        './dropdown/attachBody',
        './dropdown/minimumResultsForSearch',
        './dropdown/closeOnSelect',
        './dropdown/select2Utils',
        './results',
        './results/querying',
        './results/tags',
        './results/select2Utils',
        './translation',
        './diacritics'
    ], function ($, Utils, Keys, SelectData, ArrayAdapter, AjaxAdapter, SingleSelection, MultipleSelection, Placeholder, AllowClear, SelectionSearch, EventRelay, OpenToSelect, UnselectClose, SelectOnClose, Dropdown, DropdownSearch, HideSelected, InfiniteScroll, AttachBody, MinimumResultsForSearch, CloseOnSelect, Select2Utils, Results, ResultsQuerying, Tagger, ResultsSelect2Utils, Translation, Diacritics) {

        var Select2 = function ( $element, options ) {
            // ... Select2 initialization ...
        };

        // ... Select2 prototype methods ...

        $.fn.select2 = function () {
            // ... jQuery plugin initialization ...
        };

        $.fn.select2.amd = S2;
    });
}
}));